import * as React from 'react';
import { Time } from './Time';

export default () => (
	<main className="container my-5">
		<h1 className="text-primary text-center">Hello SSE!</h1>
		<Time />
	</main>
);
